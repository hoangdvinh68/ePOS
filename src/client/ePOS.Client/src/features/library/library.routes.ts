import { Routes } from '@angular/router';
import { LibraryComponent } from '@efeatures/library/library.component';

export const routes: Routes = [
  {
    path: '',
    component: LibraryComponent,
  },
];
